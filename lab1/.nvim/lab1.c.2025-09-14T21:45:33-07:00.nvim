#include <stdio.h>
#include <stdlib.h>

int main() {
  char *line = NULL;
  size_t line_len = 0;
  ssize_t wline = getline(&line, &line_len, stdin);

  printf("here\n");
  printf("%b \n",wline );
  return 0;
}
