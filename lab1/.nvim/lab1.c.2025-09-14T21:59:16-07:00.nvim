#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
  char *line = NULL;
  size_t line_len = 0;
  ssize_t wline = getline(&line, &line_len, stdin);
  
  printf("The following Line: %c", &wline[1]);

  printf("here\n");
  free(line);
  return 0;
}
